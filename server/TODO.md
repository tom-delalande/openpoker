- Split pots

- Better matchmaking
  - Continuous tables
  - Force blinds depending on hands since last big blind
  - Private games
- Add some delay to the showdowns and cards dealt

- Delete finished table from Redis -> Do this later
- The same player can join the same table twice and break everything
- Stack isn't updated when leaving table (need to refresh)

- Request action breaks if someone leaves mid-hand
- Weird bug where one player is set to winner mid-hand while the other player takes all actions